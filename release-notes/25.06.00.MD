## Aspen LiDA Updates
### Account Updates
- The last used sort method for checkouts, available holds, and unavailable holds will now be remembered and preferred
  when viewing holds and checkouts, and sync between Discovery and LiDA. (DIS-800) (*KK, MDN*)
- When checking out a title with linked accounts, ensure the Checkouts page updates properly after the checkout completes. (DIS-944) (*MDN*)

### Boundless Updates
- Correct Access Online Link within checkouts to refer to Boundless rather than Axis 360. (DIS-771) (*MDN*)

### Library Card Updates
- increasing font size of text under barcode (DIS-751) (*IT*)
- remove text being passed into barcode component and adding a separate text component below it so we can modify the size of the text (DIS-751) (*IT*)

### Scan & Go Updates
- Display message while processing checkout to let user know they can scan the next item. (DIS-773) (*KP*)
- Allow user to type in barcode instead of scanning in Scan & Go. (DIS-780) (*KP*)

### Other Updates
- Target specific XCode version during build to work around build compatibility issue with React Native and Expo. (DIS-762) (*MDN*)
- Cleanup of query caching for checkouts. (DIS-944) (*MDN*)
